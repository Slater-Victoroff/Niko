rollout_steps: 6
anchor_target: true
param_encoder:
  name: rb_param
  cond_dim: 32
  h_dim: 64

encoder:
  name: sequence_conv
  in_channels: 4
  context_frames: 6
  latent_dim: 16
  hidden_dim: 64
  cond_dim: 32

# operator:
#   name: linear_local
#   latent_dim: 16

operator:
  name: advection_diffusion
  latent_dim: 16
  hidden_dim: 64
  cond_dim: 32

decoder:
  name: shared_heads
  latent_dim: 16
  hidden_dim: 48
  upsample: 2
  zero_mean_pressure: true
  use_streamfunction: true
